package com.coherentsolutions.section2;

// This code snippet is for demonstration purposes and cannot be run directly.
// It illustrates how Java uses type intersections and generics internally.

enum Color {
    RED, GREEN, BLUE;
}

// Example of type intersection in Java's Enum
// Enum<E extends Enum<E>> implements Comparable<E>, Serializable

